global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'dwaarpal'
    static_configs:
      # Use host.docker.internal to reach the Go server running on the host machine
      - targets: ['host.docker.internal:8080']
